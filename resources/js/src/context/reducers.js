import { USER_LOADED } from "./types";


export const userReducer = (state, action) => {
    const {} = action;

    switch (type) {
        case USER_LOADED:
            return;
        default:
            return;
    }
}